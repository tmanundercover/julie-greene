{
  "name": "transform-hw-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "import:default": "sanity dataset import data/default-content.ndjson production --replace",
    "import:event": "sanity dataset import data/upcoming-event.ndjson production --replace"
  },
  "dependencies": {
    "@sanity/vision": "^3.70.0",
    "sanity": "^3.70.0",
    "styled-components": "^6.1.19"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
